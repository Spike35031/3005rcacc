box: debian
build:
  steps:
    - arjen/hugo-build@1.11.0:
        version: "0.16"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - takuma7/gh-pages@0.2.3:
        token: $GIT_TOKEN
        domain: 3005cadets.com
        basedir: public
